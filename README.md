# iac for a table users in dynamodb
* This is a pulumi project
* Creates a dynamodb table with a hashKey `id`

    * pulumi new // to create a new project
    * pulumi up // to deploy resources
    * pulumi destroy // to remove resources